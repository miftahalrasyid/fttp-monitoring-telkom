import React from 'react';
import styles from './footer.module.css';

function footer() {
  return <div className={`wrapper ${styles.footerContainer}`}>
      design by Miftah
  </div>;
}

export default footer;
